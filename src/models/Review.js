export default class Review {
    constructor(id, bookId, username, review) {
        this.id = id;
        this.bookId = bookId;
        this.username = username;
        this.review = review;
    }
}